// All these tree properties must be defined for unknown values
// to raise error
export const forbidUnknownValues = {
  whitelist: true,
  forbidNonWhitelisted: true,
  forbidUnknownValues: true,
};
